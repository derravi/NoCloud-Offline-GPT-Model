(1) Simply Download the Ollama And pull in the requered model accordingly your systems.

(2) Make the Virtuall ENV (If Needed)

(3) Then Install the dependensis.

(4) open the terminal go inside the "backend" folder

(5) run the Apis using the below :- uvicorn main:app --host 0.0.0.0 --port 8000